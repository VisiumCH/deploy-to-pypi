name: Call a reusable workflow

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  call-workflow-deploy-to-pypi:
    uses: ./.github/workflows/cd.yaml
    # with:
    #   major_update: ${{ github.event.inputs.major_update }}